# Task 3 Report: ViewCache

## Status: DONE

## Commit
`1e7b9cd0619` — ROGER: Add ViewCache (hires view spritesheet + cel loading)

## Files Changed
- `engines/sci/roger/view_cache.h` — new: ViewCache class declaration
- `engines/sci/roger/view_cache.cpp` — new: implementation (loadLoop, getCel, readFile, destructor)
- `test/sci/roger/test_view_cache.h` — new: CxxTest suite (3 tests: cel_0_is_red_2x2, cel_1_is_green, missing_view_returns_null)
- `test/sci/roger/fixtures/gen_overlay_fixtures.py` — extended: generates `900/` subdir with view.900.loop.0.{png,json}
- `test/sci/roger/fixtures/900/view.900.loop.0.{json,png}` — new fixture files
- `engines/sci/module.mk` — added `roger/view_cache.o`
- `build_tests.ps1` — added `view_cache.cpp` to `$RogerSources`, `test_view_cache.h` to `$RogerTestHeaders`

## Test Summary
`.\build_tests.ps1` → **Running cxxtest tests (12 tests)............OK!** (TESTS PASSED, exit 0)
Prior count: 9. New count: 12. TestViewCache adds 3 tests; all pass.

## RED → GREEN
- RED: Build failed with `C2079: 'Common::Hash<Key>' undefined` (missing `common/hash-str.h` in view_cache.h), then `C2228: left of '.exists' must have class/struct/union` (MSVC most-vexing-parse on `Common::FSNode node(Common::Path(path))`).
- Fixes applied: added `#include "common/hash-str.h"` to view_cache.h; split FSNode construction onto two lines (`Common::Path fsPath(path); Common::FSNode node(fsPath);`) matching the pattern in png_loader.cpp.
- GREEN: All 12 tests pass, no new errors, pre-existing warnings only (C4146, C4334, C4506, C4611 — all in non-Roger files, unchanged from prior builds).

## Concerns
None. Implementation is complete and tested. The brief's single-line `Common::FSNode node(Common::Path(path))` triggers MSVC's most-vexing-parse; the two-line form is idiomatic and matches png_loader.cpp.

## Review fixes

### Changes made

1. **`engines/sci/roger/view_cache.cpp` — `loadLoop` null-safety guards:**
   - Added guard before `frames` access: if `!obj.contains("frames") || !obj["frames"]->isObject()`, frees `sheet` and `root`, returns nullptr (no leak, no crash).
   - Guarded `animations.loop` array elements: `if (arr[i] && arr[i]->isString())` before `push_back`.
   - Guarded per-cel frame lookup: checks `frames.contains(order[i])`, entry `isObject()`, `frameEntry.contains("frame")`, `"frame"` `isObject()`, and all four `x/y/w/h` keys exist and `isIntegerNumber()` before dereferencing — skips the cel (continue) on any failure.

2. **`engines/sci/roger/view_cache.h`:** Added `ViewCache(const ViewCache &) = delete;` and `ViewCache &operator=(const ViewCache &) = delete;` to prevent accidental copy of the raw-pointer-owning class.

3. **`test/sci/roger/fixtures/gen_overlay_fixtures.py`:** Added view 901 — a small 2×2 PNG with a JSON that has `animations` and `meta` but no `"frames"` key (deliberately malformed). Generates `901/view.901.loop.0.{png,json}`.

4. **`test/sci/roger/fixtures/901/view.901.loop.0.{png,json}`:** New fixture files generated by running the updated script.

5. **`test/sci/roger/test_view_cache.h`:** Added `test_malformed_json_returns_null` — asserts `cache.getCel(901, 0, 0) == nullptr` without crashing.

### Build output

```
.\build_tests.ps1
  Running cxxtest tests (13 tests).............OK!
TESTS PASSED (exit 0)
```

Test count rose from 12 → 13. All 13 pass. Build output pristine (no new warnings).
